deploy:
    needs: build-analyze-scan
    runs-on: aws-netflix # Use your self-hosted runner label here
    - name: Run the container
        run: docker run -d --name myntra -p 3000:3000 mouni9948/myntra:latest
